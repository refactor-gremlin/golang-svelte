id: svelte-legacy-slots
message: "Legacy slots are deprecated in Svelte 5; use snippets instead."
severity: warning
language: svelte
rule:
  any:
    - kind: svelte_raw_text
      regex: '<slot[^>]*>.*</slot>'
    - kind: svelte_raw_text
      regex: '<slot[^>]*/>'
